<hr />
<p>
	<strong>Related ZenDesk Tickets</strong>
</p>
<ul>
	<% if @issue.related_zendesk_tickets.count > 0 %>
	<% @issue.related_zendesk_tickets.each do |ticket| %>
		<li><%= link_to "Ticket #{ticket[:id]}", "https://#{@issue.zendesk_subdomain}.zendesk.com/agent/tickets/#{ticket[:id]}" %> (<%= ticket[:status] %>) - <%= ticket[:subject] %></li>
	<% end %>
	<% else %>
	<li>No related tickets.</li>
	<% end %>
</ul>
