<hr />
<p>
  <strong>Related Zendesk Tickets (<%= @issue.related_zendesk_tickets.count %>)</strong>
</p>
<% if @issue.related_zendesk_tickets.count > 0 %>
<table class="list issues">
  <thead>
    <tr>
      <th>Zendesk ID</th>
      <th>Status</th>
      <th>Subject</th>
      <th>Created</th>
      <th>Updated</th>
    </tr>
  </thead>
  <tbody>
    <% @issue.related_zendesk_tickets.each do |ticket| %>
    <tr>
      <td class="id"><%= link_to ticket[:id], "https://#{@issue.zendesk_subdomain}.zendesk.com/agent/tickets/#{ticket[:id]}" %>
      <td class="status"><%= ticket[:status] %></td>
      <td class="subject"><%= link_to ticket[:subject], "https://#{@issue.zendesk_subdomain}.zendesk.com/agent/tickets/#{ticket[:id]}" %></td>
      <td class="created_at"><%= ticket[:created_at].strftime('%m/%d/%Y') %></td>
      <td class="updated_at"><%= ticket[:updated_at].strftime('%m/%d/%Y') %></td>
    </tr>
    <% end %>
  </body>
</table>
<% else %>
<p>No related tickets.</p>
<% end %>
